# 작업 기록

## 2026-08-06 — 봇 미러가 그리던 가짜 판 잡기

- 변경 파일: `packages/engine/src/mirror.ts`, `packages/engine/src/replay.ts`, `packages/engine/tests/mirror.test.ts`, `apps/backend/README.md`, `apps/backend/src/protocol.ts`, `apps/frontend/src/net/protocol.ts`
- 요약: 외부 봇 화면이 뒤죽박죽으로 그려지던 문제를 진단하고, 미러의 어긋남 탐지를 조각 수 비교에서 상태 지문 비교로 바꿨다. 시작 카운트다운 규약을 문서·프로토콜에 명시했다.

### 증상과 진단

harubot이 붙은 대전에서 (1) 봇 보드가 라인 하나 못 지운 채 색만 뒤섞여 꽉 차 보이고 (2) 카운트다운 전에 봇만 먼저 움직였다.

로컬에 릴레이 서버와 참조 봇 러너를 띄우고, 사람 클라이언트와 똑같이 `sync`를 받아 `BoardMirror`로 되돌린 뒤 봇이 2초마다 보내는 `full` 키프레임과 대조하는 진단기를 만들어 갈랐다(`~/Bufs/Claude Files/2026-08-06/mirror-diag.mjs`). 정상 봇은 12/12 일치·첫 하드드롭 244프레임. 봇을 "카운트다운 무시"로 개조하자 0/9 일치에 첫 하드드롭 19프레임으로, 스크린샷 증상이 그대로 재현됐다. **두 증상은 하나의 원인이다** — 봇이 `READY_FRAMES`(240)를 지키지 않으면 미러는 자기 Ready 구간에서 들어온 입력을 흘려보내고, 그 뒤로 프레임 기준이 어긋난 채 재생돼 조각이 엉뚱한 자리에 놓인다.

### 함정 — 개수가 같으면 어긋남이 안 잡혔다

`BoardMirror.stale()`이 `piecesPlaced`만 견주고 있었다. 주석은 "그럴듯한 가짜 판"을 막겠다고 써 있었지만, **상대와 미러가 같은 박자로 두면 조각 수는 늘 맞아떨어져서** 자리만 다른 어긋남을 통째로 놓쳤다. 위 실험에서 9번 중 9번 다 통과시켰다.

`snapshotFingerprint`(기존 `fingerprint`를 스냅샷에서도 쓸 수 있게 쪼갠 것)로 상태 전체를 견주도록 바꿨다. 다만 **같은 프레임에 서서** 비교해야 한다 — 미러는 스트림보다 늘 몇 프레임 뒤에 있어 받은 그 자리에서는 견줄 것이 없다. 그래서 키프레임을 `pending`으로 들고 있다가 `advance()`가 그 프레임에 닿는 순간 대조한다. 받자마자 강제로 감아 비교하는 방법도 있었지만 렌더 리듬을 깨서 버렸다.

고친 뒤 카운트다운 무시 봇은 240프레임 이후 전 구간이 교정됐고, 시드 불일치 봇은 첫 키프레임에서 큐까지 되돌아가 그 뒤로 정확해졌다.

### 남은 것

`match-start`를 받고 240프레임 세는 규약이 엔진 상수에만 있고 프로토콜 어디에도 없었다. 문서·타입 주석에 명시했지만, 시작 신호 자체를 `match-start`(준비)와 별도 개시 신호로 나누는 편이 낫다는 이야기가 나왔다 — 미결.
